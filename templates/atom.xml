<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site_title }}</title>
    <link href="{{ site_url }}/atom.xml" rel="self" />
    <link href="{{ site_url }}/" />
    <id>{{ site_url }}/</id>
    <updated>{{ feed_updated }}</updated>
    <author>
        <name>{{ site_title }}</name>
    </author>

    {% for item in items %}
    <entry>
        <title>{{ item.title }}</title>
        <link href="{{ site_url }}{{ item.url }}" />
        <id>tag:{{ site_domain }},{{ item.date.isoformat() }}:{{ item.url }}</id>
        <updated>{{ item.date.isoformat() }}T12:00:00Z</updated>
        <summary>{{ item.category }} update.</summary>
        <content type="html">{{ item.content | forceescape }}</content>
    </entry>
    {% endfor %}
</feed>
