<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site_title }}</title>
    <link href="{{ site_url }}/atom.xml" rel="self" />
    <link href="{{ site_url }}/" />
    <id>{{ site_url }}/</id>
    <updated>{{ feed_updated }}</updated>
    <author>
        <name>{{ site_title }}</name>
    </author>

    {% for entry in items %}
    <entry>
        <title>{{ entry.content.title }}</title>
        <link href="{{ site_url }}{{ entry.content.url }}" />
        <id>{{ site_url }}{{ entry.content.url }}</id>
        <updated>{{ entry.feed_updated }}</updated>
        <published>{{ entry.content.published_date }}</published>
        <summary>{{ entry.content.category }} update - {{ entry.content.date_display }}</summary>
        <content type="html">{{ entry.content.content | forceescape }}</content>
    </entry>
    {% endfor %}
</feed>
